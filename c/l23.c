#include <stdio.h>
void main()

{
    int age[4] = {1, 2, 3, 4};
    
        //for (int i = 0; i < 4; i++)
        //{
            /* code */

           // printf("enter the %d number of array" , i);
           // scanf("%d", &age[i]);
        //}
for (int i = 0; i < 4; i++)
{
printf("the %d number of array is %d\n", i, age[i]);
}
}